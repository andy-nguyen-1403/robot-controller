services:
  robot-api:
    image: ${ROBOT_IMAGE}
    container_name: robot-api-production
    ports:
      - "8081:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_HTTP_PORTS: 8080
    restart: unless-stopped
